Para instalar todas las dependecias:
  pip3 install -r requirements.txt
	(da error en algo pycayro, no hace falta prestarle atencion)

Por defecto genera 200 imagenes distintas, para otro numero cambiar
el main.

PARA EJECUTAR

"python3 get_images.py"
